# Historical Faith Narrative - Python Dependencies
# Install with: pip install -r requirements.txt

# Core dependencies
python-dotenv>=1.0.0
requests>=2.31.0

# OpenAI (for embeddings)
openai>=1.0.0

# Supabase (database)
supabase>=2.0.0

# Optional but recommended
httpx>=0.25.0          # Modern HTTP client (used by some packages)
tenacity>=8.2.0        # For retry logic
aiohttp>=3.9.0         # Async HTTP support

# Development/testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
